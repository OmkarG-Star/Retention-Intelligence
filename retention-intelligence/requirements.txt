# Core
pandas>=2.1,<4
numpy>=1.26,<3
scikit-learn>=1.4
scipy>=1.11

# Gradient boosting. Strongly recommended: LightGBM gives exact SHAP values
# through pred_contrib, so the explainability layer needs no extra dependency.
# Without it the code falls back to scikit-learn's HistGradientBoosting.
lightgbm>=4.1

# API + server
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Storage / IO
pyarrow>=14.0
openpyxl>=3.1

# Optional, auto-detected at runtime:
#   shap>=0.44        alternative explainer when LightGBM is unavailable
#   lifelines>=0.27   Kaplan-Meier reference curves
